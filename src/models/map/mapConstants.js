
const mapWidth = 20, mapHeight = 20 // map width and height are the number of tiles, not pixels
const tileWidth = 40, tileHeight = 40// tile width and height are in pixels

export { mapWidth, mapHeight, tileWidth, tileHeight }