
const mapWidth = 100, mapHeight = 100// map width and height are the number of tiles, not pixels
const viewportHeight = 1000, viewportWidth = 2100
const tileWidth = 32, tileHeight = 32// tile width and height are in pixels

export { mapWidth, mapHeight, viewportHeight, viewportWidth, tileWidth, tileHeight }