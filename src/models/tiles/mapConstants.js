
const mapWidth = 60, mapHeight = 35// map width and height are the number of tiles, not pixels
const tileWidth = 32, tileHeight = 32// tile width and height are in pixels

export { mapWidth, mapHeight, tileWidth, tileHeight }