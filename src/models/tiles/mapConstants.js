
const mapWidth = 50, mapHeight = 50// map width and height are the number of tiles, not pixels
const viewportHeight = 800, viewportWidth = 2000

export { mapWidth, mapHeight, viewportHeight, viewportWidth }